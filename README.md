# React To-Do List SPA

A simple **Single Page Application (SPA)** built with **React** and **React Router**, featuring a functional **To-Do List** with client-side routing and reusable components.

The project demonstrates core React concepts such as state management, component composition, and navigation without page reloads.

---

## 🚀 Features

- ✅ Single Page Application (SPA)
- ✅ Client-side routing with **react-router-dom**
- ✅ Add and remove tasks dynamically
- ✅ Remove tasks with a single click
- ✅ Keyboard support (press Enter to add a task)
- ✅ Shared header with navigation
- ✅ Clean and minimal UI
- ✅ Component-based architecture

---

## 🧭 Pages & Routes

| Route      | Component | Description                  |
|------------|-----------|------------------------------|
| `/`        | Todo      | Home page with To-Do List    |
| `/about`   | About     | About page                   |
| `/contact` | Contact   | Contact page                 |

---

## 🛠 Technologies Used

- **React**
- **React Router DOM**
- **JavaScript (ES6+)**
- **JSX**
- **HTML5 & CSS3**
- **Vite** (or Create React App, depending on setup)

---

## 🧠 How the To-Do List Works

- Tasks are stored in React state using `useState`
- Users can:
  - Type a task in the input field
  - Press **Enter** or click **Add task** to add it
  - Click on a task to remove it
- The task list updates instantly without page reload

---

## 📁 Project Structure

